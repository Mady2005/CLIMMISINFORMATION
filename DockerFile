# Start with a standard Python environment
FROM python:3.9-slim

# Set up a working directory inside the container
WORKDIR /code

# Copy the requirements file into the container
COPY ./requirements.txt /code/requirements.txt

# Install the libraries from the requirements file
RUN pip install --no-cache-dir --upgrade -r /code/requirements.txt

# Copy all your other application files into the container
COPY . /code/

# Tell the container to run your Gradio app when it starts
CMD ["python", "app.py"]